% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_vaccine.R
\name{vaccine_scenario}
\alias{vaccine_scenario}
\alias{generate_vaccine}
\alias{vaccine_scenario_defaults}
\alias{vaccine_scenario_base_defaults}
\alias{vaccine_scenario_tweak}
\alias{vaccine_scenario_set_beta_A1_relative}
\alias{vaccine_scenario_set_gamma_0}
\alias{vaccine_scenario_set_true_eff}
\alias{vaccine_scenario_set_samplesize}
\title{Generate Data for Vaccine Scenario}
\usage{
vaccine_scenario(
  print = interactive(),
  scenario_defaults = vaccine_scenario_defaults
)

generate_vaccine(condition, fixed_objects = list(include_unobserved = FALSE))

vaccine_scenario_defaults()

vaccine_scenario_base_defaults()

vaccine_scenario_tweak(..., scenario_defaults = vaccine_scenario_base_defaults)

vaccine_scenario_set_beta_A1_relative(Design, diff = -0.2)

vaccine_scenario_set_gamma_0(Design, r = 1)

vaccine_scenario_set_true_eff(Design)

vaccine_scenario_set_samplesize(
  Design,
  alpha = 0.025,
  CSE = 0.3,
  VE_H1 = 0.7,
  power = 0.8,
  r = 1
)
}
\arguments{
\item{print}{print code to generate parameter set?}

\item{scenario_defaults}{A function returning a named \code{list} of parameter
defaults. By default \code{vaccine_scenario_base_defaults()} is used.}

\item{condition}{Row of parameters dataset}

\item{fixed_objects}{other objects passed to simulation runs}

\item{...}{Named parameter overrides. Each name must correspond to an element
returned by \code{scenario_defaults}. Supplied values replace the defaults. These
may be scalars or vectors; vector inputs generate a small parameter grid.}

\item{Design}{Design dataset as returned by \code{vaccine_scenario}}

\item{diff}{Difference in vaccine efficacy between dose 1 and dose 2}

\item{r}{for setting gamm_0 and sample size calculation: allocation ratio}

\item{alpha}{for sample size calculation: one-sided alpha level}

\item{CSE}{for sample size calculation: super-superiority margin on the VE scale}

\item{VE_H1}{vaccine efficacy assumed for sample size planning}

\item{power}{for sample size calculation: target power}
}
\value{
For \code{vaccine_scenario}: a design tibble with default values invisibly

For \code{generate_vaccine}: a simulated dataset

For \verb{vaccine_scenario_defaults:} A named list of default parameter
values for use with \code{vaccine_scenario_grid()}.

For \verb{vaccine_scenario_base_defaults:}
A named list of baseline parameter values for use with
\code{vaccine_scenario_tweak()}. Each element is a scalar and together they define
a single reference scenario.

For \code{vaccine_scenario_tweak}:
A \code{data.frame} containing the resulting scenario design for use with
\code{generate_vaccine}.

For \code{vaccine_scenario_set_beta_A1_relative}: The Design dataset with added column \code{beta_A1}

For \code{vaccine_scenario_set_gamma_0}: The Design dataset with the added column gamma_0

\code{vaccine_scenario_set_true_eff}: the Design dataset with the added column rr_ps

For \code{vaccine_scenario_set_samplesize}: the Design dataset with the added columns n_trt and n_ctrl
}
\description{
Generate Data for Vaccine Scenario
}
\details{
vaccine_scenario generates a default design \code{data.frame}
for use with \code{generate_vaccine}. If print is \code{TRUE} code to produce
the template is also printed for copying, pasting and editing by the user.
(This is the default when run in an interactive session.)
\code{vaccine_scenario_grid()} constructs a simulation design by evaluating the
named parameter list returned by \code{scenario_defaults} via
\code{params_scenarios_grid()}. It then derives the parameter \code{beta_A1} from
\code{beta_A2} and \code{effect_before_d2}.

If \code{print = TRUE}, the function also prints code that reproduces the same
parameter grid specification. This is intended to make it easy to copy,
modify, and reuse scenario settings.

The default settings generate a factorial grid across the vector-valued
elements returned by \code{vaccine_scenario_defaults()}. A custom defaults function
can be supplied to generate an alternative grid.

This function returns the default parameter settings used to generate the
standard simulation design for \code{generate_vaccine}. In contrast to
\code{vaccine_scenario_base_defaults()}, several entries are vectors and therefore
define a multi-row parameter grid when passed to \code{params_scenarios_grid()}.

The returned list is primarily intended as an internal or user-modifiable
source of defaults that can be supplied via the \code{scenario_defaults} argument
of \code{vaccine_scenario_grid()}.

\code{vaccine_scenario_base_defaults} returns a one-value-per-parameter baseline configuration for
\code{generate_vaccine}. It is intended for targeted testing and debugging, where a
user starts from a single reference scenario and modifies only selected
parameters.

In contrast to vaccine_scenario_defaults(), the returned list does not
define a broad factorial grid unless the user explicitly replaces one or more
scalar values by vectors in vaccine_scenario_tweak().

\code{vaccine_scenario_tweak()} is intended for targeted testing. It starts from a
baseline scenario returned by \code{scenario_defaults}, replaces only the
parameters supplied in \code{...}, and then evaluates the resulting settings with
\code{params_scenarios_grid()}.

By default the function starts from the scalar baseline returned by
\code{vaccine_scenario_base_defaults()}, so changing only a few arguments typically
produces a single scenario row. If one or more supplied overrides are vectors,
a corresponding parameter grid is generated.

The function validates that all supplied argument names are known parameters.
An error is thrown if unknown names are passed.

As in \code{vaccine_scenario_grid()}, \code{beta_A1} is derived from \code{beta_A2} and
\code{effect_before_d2}.
}
\section{Functions}{
\itemize{
\item \code{vaccine_scenario()}: Generate default assumptions \code{data.frame}

\item \code{generate_vaccine()}: Simulate a dataset from the vaccine scenario

\item \code{vaccine_scenario_defaults()}: Default parameter values for the full vaccine simulation scenario grid

\item \code{vaccine_scenario_base_defaults()}: Default parameter values for a single baseline vaccine simulation scenario

\item \code{vaccine_scenario_tweak()}: Modify selected vaccine scenario parameters starting from a base scenario

\item \code{vaccine_scenario_set_beta_A1_relative()}: Calculate VE after dose 1 as fraction of VE after dose 2

\item \code{vaccine_scenario_set_gamma_0()}: Calculate gamma_0 from other parameters

\item \code{vaccine_scenario_set_true_eff()}: Calculate relative risk in the principal stratum of always compliers

\item \code{vaccine_scenario_set_samplesize()}: Calculate sample size for assumed VE

}}
\examples{
Design <- vaccine_scenario() |>
  vaccine_scenario_set_beta_A1_relative() |>
  vaccine_scenario_set_gamma_0() |>
  vaccine_scenario_set_true_eff() |>
  vaccine_scenario_set_samplesize()
Design

generate_vaccine(Design[1,])
defaults <- vaccine_scenario_defaults()
str(defaults)
defaults$beta_A2

base_defaults <- vaccine_scenario_base_defaults()
str(base_defaults)
base_defaults$overall_compliance

# single modified test scenario
Design <- vaccine_scenario_tweak(
  beta_A2 = log(1 - 0.5),
  overall_compliance = 0.8
)
Design

# small grid around the baseline scenario
Design_grid <- vaccine_scenario_tweak(
  beta_A2 = log(1 - c(0.3, 0.5, 0.7)),
  effect_before_d2 = c(TRUE, FALSE)
)
Design_grid

}
